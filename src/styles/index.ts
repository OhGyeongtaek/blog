import styled from "@emotion/styled";

export const IndexContainer = styled.main`
  max-width: 740px;
  min-height: 600px;
  padding-top: 40px;
  margin: 0 auto;

  & .chip-group .chip {
    margin: 4px;
  }
`;
